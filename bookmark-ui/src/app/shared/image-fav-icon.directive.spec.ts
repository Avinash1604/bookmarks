import { ImageFavIconDirective } from './image-fav-icon.directive';

describe('ImageFavIconDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageFavIconDirective();
    expect(directive).toBeTruthy();
  });
});
